# This is the Dockerfile for the service.
# It defines the environment and dependencies required to run the application inside a container.
#
# Key steps:
# - Uses an official Python base image.
# - Sets up the working directory inside the container.
# - Copies necessary files and installs dependencies.
# - Exposes the required port for external access.
# - Defines the command to start the application.

FROM python:3.11